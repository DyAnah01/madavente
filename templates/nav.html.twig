<nav class="navbar navbar-expand-lg bg-body-tertiary text-center justify-content-center navig">
  <div class="container-fluid">
    <a class="navbar-brand mx-5" href=" {{ path('home') }} ">Home</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav">

        {# <li class="nav-item">
          <a class="nav-link" href="#">Features</a>
        </li>#}
        <li class="nav-item dropdown mx-5">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">{%
            if app.user and app.user.roles[0] == "ROLE_ADMIN" %}Gestions des articles {% else %}
            Articles{% endif %}
          </a>
          <ul class="dropdown-menu">
            {% if app.user and app.user.roles[0] == "ROLE_ADMIN" %}
            <li><a class="dropdown-item" href="#">Categorie 1</a></li>
            <li><a class="dropdown-item" href="#">Categorie 2</a></li>
            <li><a class="dropdown-item" href="#"></a></li>

            {% else %}
            <li><a class="dropdown-item" href="#">Categorie 1</a></li>
            <li><a class="dropdown-item" href="#">Categorie 2</a></li>
            {% endif %}

          </ul>
        </li>
        {% if app.user %} 
            <li class="nav-item dropdown mx-5">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Mon Compte</a>
            <ul class="dropdown-menu">
              {% if app.user %}
                {% if app.user.roles[0] == "ROLE_ADMIN" %}
                <li><a class="dropdown-item" href="#">Liste de commande</a></li>
                {% endif %}

                {% if app.user.roles[0] == "ROLE_USER" %}
                <li><a class="dropdown-item" href="#">Historique de commande</a></li>
                {% endif %}

                <li><a class="dropdown-item" href=" {{ path('app_logout') }} ">Logout</a></li>
              {% endif %}

            {% else %}
            <li><a class="dropdown-item fs-5" href=" {{ path('app_login') }} ">Mon Compte</a></li>
            {# <li><a class="dropdown-item" href="#">Something else here</a></li> #}
          </ul>
        </li>
        {% endif %}
      </ul>

    </div>
    {% if app.user %}
    {# {{ dump(app.user) }} #}
    <div class="d-flex flex-column">
      <p class="mb-3 text-end opacity-50">
        Vous êtes connecté en tant que {{ app.user.prenom }} </p>
      {% if app.user.roles[0] == "ROLE_ADMIN" %}
      <p class="fs-3 text-danger">Espace admin</p>
      {% endif %}
    </div>
    {% endif %}

  </div>
</nav>