
{% extends 'base.html.twig' %}

{% block title %}{{ parent() }}|CategorieModifier{% endblock %}

{% block body %}
<div class="col-md-4">
    <h2>Ajouter une catégorie</h2>
    {{ form_start(formCategorie) }}
        {{ form_row(formCategorie.nom) }}
    <input type="submit" class="btn btn-success mt-3 col-md-6" value="Ajouter">
    {{ form_end(formCategorie) }}
</div>

<div class="d-flex flex-row-reverse">
    <a class="btn btn-success flex-end me-5" href="{{ path("app_categorie") }}">Retour</a>   
</div>
{% endblock %}
