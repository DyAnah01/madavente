<nav class="navbar navbar-expand-lg text-center justify-content-center navig row" style="background-color: #5a473150;">
  <div class="col-md-9 container-fluid">
    {# <a class="navbar-brand mx-5" href=" {{ path('home') }} ">Accueil</a> #}
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNavDropdown"
      aria-controls="navbarNavDropdown" aria-expanded="false" aria-label="Toggle navigation">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNavDropdown">
      <ul class="navbar-nav px-5">
        <li class="nav-item"><a class="nav-link" href="{{ path('home') }}">Accueil</a></li>
        <li class="nav-item dropdown">
          <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">{%
            if app.user and app.user.roles[0] == "ROLE_ADMIN" %}Gestions des articles {% else %}
            Articles{% endif %}
          </a>
          <ul class="dropdown-menu">
            {% if app.user and app.user.roles[0] == "ROLE_ADMIN" %}
            <li><a class="dropdown-item" href=" {{ path('app_categorie') }} ">Gestion de catégorie</a></li>
            <li><a class="dropdown-item" href="{{ path('add_articles') }}">Gestion des articles</a></li>
            <li><a class="dropdown-item" href="#"></a></li>
            {% else %}
            <li><a class="dropdown-item" href="#">Categorie 1</a></li>
            <li><a class="dropdown-item" href="#">Categorie 2</a></li>
            {% endif %}
          </ul>
        </li>
        {% if app.user and app.user.roles[0] == "ROLE_USER" %} 
        <li class="nav-item dropdown">
            <a class="nav-link dropdown-toggle" href="#" role="button" data-bs-toggle="dropdown" aria-expanded="false">Mon Compte</a>
            <ul class="dropdown-menu">
                <li><a class="dropdown-item" href="#">Informations personnelles</a></li>
                <li><a class="dropdown-item" href="#">Historique de commande</a></li>
                <li><a class="dropdown-item" href=" {{ path('app_logout') }} ">Se déconnecter</a></li>
            </ul>
        </li>
        {% else %} 
        {% endif %}
        {# <li class="nav-item text-end"><a class="nav-link" href="{{ path('app_login') }}">Se connecter</a></li> #}
        {% if app.user and app.user.roles[0] == "ROLE_ADMIN"  %}
        <li class="nav-item"><a class="nav-link" href="">Commandes en cours</a></li>
        <li class="nav-item"><a class="nav-link" href="">Historiques de vente</a></li>
        <li class="nav-item"><a class="nav-link" href=" {{ path('app_user') }} ">Liste d'utilisateur</a></li>
        {% else %}
        <li class="nav-item"><a class="nav-link" href="{{ path('get_cart') }}">Mon panier</a></li>
        {% endif %}
      </ul>
    </div>
      {# {{ dump(app.user) }} #}


  </div>
  <div class="col-md-3 d-flex flex-column pe-3">
    {% if app.user %}
    <p class="mb-3 text-end opacity-50">
      Vous êtes connecté en tant que {{ app.user.prenom }} </p>
    {% if app.user.roles[0] == "ROLE_ADMIN" %}
    <p class="fs-3 text-danger text-end">Espace admin</p>
    <a class="text-end text-decoration-none" href=" {{ path('app_logout') }} ">Se déconnecter</a>
    {% endif %}
    {% if app.user.roles[0] == "ROLE_USER" %}
    <a class="text-end text-decoration-none" href=" {{ path('app_logout') }} ">Se déconnecter</a>
    {% endif %}
  {% else %}
    <a class="text-decoration-none text-end pe-5 text-dark fs-4" href="{{ path('app_login') }}">Se connecter</a>
  {% endif %}
  </div>
</nav>